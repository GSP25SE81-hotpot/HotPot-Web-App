import { MessageNotice, AdminMessageNotice } from "./message";
import { managerRoutes, adminRoutes } from "./routes";
import { Vntext } from "./texts";

const config = {
  managerRoutes,
  adminRoutes,
  MessageNotice,
  AdminMessageNotice,
  Vntext,
};

export default config;
